Riftbound Tracker Ahri v4.9 — Icon Fix

Upload every file in this folder to the ROOT of the GitHub repository.
The filenames must remain exactly as supplied. In particular:
- index.html
- manifest.json
- sw.js
- AhriFoxIcon180_v49.png
- AhriFoxIcon192_v49.png
- AhriFoxIcon512_v49.png
- AhriFoxMaskable512_v49.png

Important:
1. Do not upload the files with names such as index(29).html, manifest(1).json or sw(1).js. GitHub Pages only uses index.html, manifest.json and sw.js.
2. The icon filenames are new in v4.9 so the phone cannot reuse the older cached icon URL.
3. Remove the old installed app before reinstalling.
4. Clear site data for the GitHub Pages address.
5. Open the website again and install/add it to the Home Screen.

Android gets a separate padded maskable icon so adaptive circular/squircle masks do not crop the fox artwork.
